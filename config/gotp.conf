# Default config file
# Set your API key here
API_KEY=test-api-key
# Where to store cached email data
CACHE_DIR=/opt/gotp-email/cache
# Set trusted proxies here (comma-separated)
TRUSTED_PROXIES=127.0.0.1/32
